import React from "react";
import { View, Text } from "react-native";
import styles from "./styles";

const RideType = (props) => {
    return (
        <View>

            <Text>not Just a uber RideType</Text>

        </View>
    );
};

export default RideType;