import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    container: {
        padding: 10,
        backgroundColor: '#eee',
        //color: 'black',
        height: '100%',
    },
    

    
});

export default styles;